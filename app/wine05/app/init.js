/**
 * a basic init of an app object that will house everything...
 * maybe create another object.
 * 
 * at least the way i'm thinking of it.
 * C prefix are for classes.
 * 
 */
var app = app || {};

// store the stuff that you aren't plannig to new
app.CModels = {};
app.CCollections = {};
app.CViews = {};
app.CTemplates = {};

// store models that you are newing.
app.Models = {};
app.Collections = {};
app.Views = {};
app.Templates = {};

// init
console.log('init app...');
